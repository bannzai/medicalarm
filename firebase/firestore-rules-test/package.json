{
  "name": "medicalarm-firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "firestore.rules の単体テスト (@firebase/rules-unit-testing + Firestore Emulator)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-medicalarm 'jest'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "jest": "^30.4.2"
  }
}
